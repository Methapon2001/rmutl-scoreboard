import 'package:flutter/material.dart';

class MyIocns extends StatelessWidget {
  const MyIocns({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('image/test.png');
  }
}
